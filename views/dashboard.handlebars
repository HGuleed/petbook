<div class="container mt-5">
  <input
    type="text"
    class="form-control m-1"
    id="inputPostContent"
    placeholder="Post here"
  />
  <button type="submit" class="btn btn-primary m-1" id="postBtn">
    Post
  </button>
</div>

{{#each post as |post|}}

<div class="container mt-5">
  {{>post-info post}}

</div>